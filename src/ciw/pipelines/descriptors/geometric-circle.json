{
  "schema": "ciw.pipeline-descriptor.v1",
  "pipeline_id": "ciw.geometric-circle.v1",
  "source_kind": "geometric-circle",
  "session_schema": "ciw.geometric-circle-session.v1",
  "summary": "GTE projection of 2D telemetry onto a declared circle with tangent covariance",
  "inputs": {
    "source_schema": "ciw.geometric-circle-source.v1",
    "upstream_kinds": [],
    "upstream_cardinality": "none",
    "configuration": "in_source"
  },
  "steps": [
    {
      "role": "gte",
      "invocation": "pinned_subprocess",
      "purpose": "geometric projection",
      "pin": {
        "revision": "e55b8be2b3ba05f7e6c6a31807c77b3e42700f07",
        "module": "geodesic_telemetry.ciw_adapter",
        "source_root": "src"
      }
    }
  ],
  "verification": {
    "method": "same_runtime_fresh_occurrence_reproduction",
    "role": null
  },
  "refusals": [
    "DECLARED_WORKLOAD_REFUSED",
    "INPUT_LIMIT",
    "INVALID_INPUT",
    "MALFORMED_RESPONSE",
    "OUTPUT_LIMIT",
    "RUNTIME_FAILED",
    "RUNTIME_IO",
    "RUNTIME_PIN_MISMATCH",
    "RUNTIME_UNAVAILABLE",
    "SOURCE_PIN_MISMATCH",
    "TIMEOUT",
    "operation_unavailable",
    "workbench_capacity"
  ],
  "domain_rules": [
    {
      "rule": "Input joint covariance must be exactly symmetric PSD in rational arithmetic (exact Schur complements, no jitter/averaging) before retention, stricter than GTE's native roundoff-tolerant domain",
      "evidence": "geometric_circle.py:95-102 calling calibrated_window._covariance (calibrated_window.py:64-76) and gte_records._covariance; policy flag at geometric_circle.py:31"
    },
    {
      "rule": "Fixed-exact circle applicability: constraint frame equals observation frame, kind circle, euclidean metric, radius>0, validity window [start,end) contains every sample time, no observed point equals the circle centre, geometry_uncertainty fixed_exact",
      "evidence": "geometric_circle.py:103-114"
    },
    {
      "rule": "Typed GTE result must echo request bindings (source_id, observation ids, times, frame, observed points, constraint, policy) and full-batch selection",
      "evidence": "adapters/gte_records.py:51-70 (continues to :132)"
    },
    {
      "rule": "The GTE reconciliation status must follow from the policy thresholds (held with reasons correction_limit_exceeded or linearization_limit_exceeded, else eligible), and reconciled_points must be null when held (adapters/gte_records.py:113-121). This is a hand-written provider-payload validator shared with geodesic.py (geodesic.py:154-161).",
      "evidence": "added by adversarial verification; citations in the rule"
    },
    {
      "rule": "The selection-support convention: duration = last sample + 1 s as half-open selection support, not a measured period (geodesic.py:71-77). It is used only to build the synthetic run that the validator needs.",
      "evidence": "added by adversarial verification; citations in the rule"
    },
    {
      "rule": "The input covariance is checked in two different domains on the same matrix. The first is exact rational PSD (geometric_circle.py:100 -> calibrated_window.py:64-76). The second is a float correlation-eigenvalue check with 1e-10 tolerance (geometric_circle.py:101 -> gte_records.py:27-48). The descriptor must say that both apply.",
      "evidence": "added by adversarial verification; citations in the rule"
    }
  ],
  "investigations": [
    "geometry-bim"
  ],
  "surface": "entry",
  "authority": "read_only",
  "implementation": {
    "module": "ciw.geometric_circle",
    "runner": "declared_workflow",
    "delegates": []
  },
  "guide": "docs/REMAINING_MODULES.md"
}
