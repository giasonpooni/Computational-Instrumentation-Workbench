{
  "schema": "ciw.pipeline-descriptor.v1",
  "pipeline_id": "ciw.flat-torus-reference.v1",
  "source_kind": "flat-torus-reference",
  "session_schema": "ciw.flat-torus-reference-session.v1",
  "summary": "Native flat-torus lattice and winding trajectory reference",
  "inputs": {
    "source_schema": "ciw.flat-torus-reference-source.v1",
    "upstream_kinds": [],
    "upstream_cardinality": "none",
    "configuration": "in_source"
  },
  "steps": [
    {
      "role": "ftr",
      "invocation": "pinned_subprocess",
      "purpose": "flat-torus reference",
      "pin": {
        "revision": "dc918562cd9e351a65475d29f46963c9f2fd7db8",
        "source_tree": "1f082c6b443f2e301ac5889122912f26968ae756",
        "module": "flat_torus.companion",
        "source_root": "src"
      }
    }
  ],
  "verification": {
    "method": "same_runtime_fresh_occurrence_reproduction",
    "role": null
  },
  "refusals": [
    "GEODESIC_REFERENCE_REFUSED",
    "INPUT_LIMIT",
    "INVALID_INPUT",
    "MALFORMED_RESPONSE",
    "OUTPUT_LIMIT",
    "RUNTIME_FAILED",
    "RUNTIME_IO",
    "RUNTIME_PIN_MISMATCH",
    "RUNTIME_UNAVAILABLE",
    "SOURCE_PIN_MISMATCH",
    "TIMEOUT",
    "operation_unavailable",
    "workbench_capacity"
  ],
  "domain_rules": [
    {
      "rule": "The native start must equal the declared cover point modulo the retained lattice. alpha and beta must be integers within 1e-12, and the reduced coordinates must lie in [0,1). Lattice orientation must have omega1 real and positive and Im(omega2) > 0.",
      "evidence": "src/ciw/geodesic_reference.py:196-208"
    },
    {
      "rule": "Trajectory consistency: exactly `samples` path parameters strictly increasing from 0 to 1. Both point lists start within 1e-12 of the reference start. Crossings are ordered in (prev,1], their count is at most |m|+|n|, and lattice steps lie in {-1,0,1}. The trajectory's lattice, winding, start, length and closed fields agree with the reference.",
      "evidence": "src/ciw/geodesic_reference.py:220-254"
    },
    {
      "rule": "Native geometry invariants: closed loop, zero Gaussian curvature, positive area and length, arclength interval [0,length], plus a fixed handoff object and a fixed list of three limitation strings.",
      "evidence": "src/ciw/geodesic_reference.py:209-219"
    },
    {
      "rule": "Provider invocation shim. It builds complex tau and start, unpacks the winding, calls flat_geodesic_reference and trace_closed_geodesic, and serialises dataclasses, ndarrays and complex values to JSON.",
      "evidence": "src/ciw/geodesic_reference.py:134-155,171"
    },
    {
      "rule": "The shim encodes complex values as {'re','im'} (src/ciw/geodesic_reference.py:143), and _pair and _check_flat assume that encoding (geodesic_reference.py:172-174,188-190). The validator and the invocation shim are coupled, so they have to be versioned together as one plugin; they cannot be split into a descriptor shim plus a generic payload schema.",
      "evidence": "added by adversarial verification; citations in the rule"
    }
  ],
  "investigations": [
    "geometry-bim"
  ],
  "surface": "inner",
  "authority": "read_only",
  "implementation": {
    "module": "ciw.geodesic_reference",
    "runner": "generic_runner",
    "delegates": []
  },
  "guide": "docs/GEODESIC_REFERENCES.md"
}
