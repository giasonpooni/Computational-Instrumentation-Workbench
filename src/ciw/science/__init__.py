"""Canonical scientific state, experiments, evidence and authority for the workbench.

The package is language-neutral at its boundary: every record is finite JSON
with an explicit schema identifier, so Julia, Rust or LaTeX tooling can consume
the same experiment specifications and ledgers without Python objects.
"""
